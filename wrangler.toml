name = "foxcode-cache-proxy"
main = "worker.js"
compatibility_date = "2024-01-01"

# 环境变量（可在 Cloudflare Dashboard 中配置，或使用 wrangler secret）
# [vars]
# TARGET_HOST = "code.newcli.com"
# USER_ID = "openclaw-user"
# RETRY_MAX = "3"
# RETRY_DELAY = "1000"
# RETRY_MAX_DELAY = "10000"
# TIMEOUT_MS = "180000"
